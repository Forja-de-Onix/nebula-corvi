services:
  node-red:
    image: nodered/node-red:latest
    container_name: node-red
    restart: unless-stopped
    ports:
      - "1880:1880"
    volumes:
      - {{ nebula_base_path }}/node-red/data:/data
    networks: [proxy]

networks:
  proxy:
    external: true