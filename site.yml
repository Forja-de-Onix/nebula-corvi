- hosts: nebula
  vars:
    servicios_compose: []
  roles:
    - base
  tasks:
    - name: Desplegar servicios estándar (rol genérico)
      include_role:
        name: compose_service
      vars:
        service_name: "{{ item }}"
      loop: "{{ servicios_compose }}"