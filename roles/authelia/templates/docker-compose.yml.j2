services:
  authelia:
    image: authelia/authelia:latest
    container_name: authelia
    restart: unless-stopped
    ports:
      - "9091:9091"
    volumes:
      - {{ nebula_base_path }}/authelia/config:/config
    networks: [proxy]

networks:
  proxy:
    external: true